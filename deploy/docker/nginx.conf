# Forcing rebuild
upstream php-fpm {
    server 127.0.0.1:9000;
}

server {
    listen 80;
    server_name localhost;
    root /var/www/html;

    index index.php index.html;

    # --- Real IP Config (important) ---
    # Trust Cloudflare IPs
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 104.16.0.0/12;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 131.0.72.0/22;

    # IPv6 ranges
    set_real_ip_from 2400:cb00::/32;
    set_real_ip_from 2606:4700::/32;
    set_real_ip_from 2803:f800::/32;
    set_real_ip_from 2405:b500::/32;
    set_real_ip_from 2405:8100::/32;
    set_real_ip_from 2a06:98c0::/29;
    set_real_ip_from 2c0f:f248::/32;

    # Also trust your Docker bridge network
    set_real_ip_from 172.17.0.0/16;
    set_real_ip_from 172.19.0.0/16;

    # Use Cloudflare’s header
    real_ip_header CF-Connecting-IP;
    real_ip_recursive on;

    # --- Normal config below ---

    # Allow access without .php extension
    location / {
        try_files $uri $uri/ $uri.php$is_args$args /index.php?$query_string;
    }

    # Cron/CLI-only scripts — never reachable over HTTP.
    # ^~ so this prefix match wins over the `~ \.php$` regex below; the scripts
    # also guard themselves via functions/cli_guard.php.
    location ^~ /scripts/ {
        deny all;
        return 404;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php-fpm;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }
}
